let a = true;
let b = false;
let c = true;
let d = false;

// Operator OR = akan true jika salah 1 nya ada true
console.log(a || b);
console.log(b || d || a);

// Operator AND = akan bernilai fdale jika salah satunya ada false
console.log(a && b && c);

// Operator pembanding 
let x = 8;
let y = 7;

    // Lebih besar
    console.log(x > y);

    // lebih kecil
    console.log(x < y);

    // sama dengan
    console.log(x == y);

    // Lebih besar samadengan
    console.log(x >= y);

    // Lebih kecil samadengan
    console.log(x <= y);

    // sama dengan 3 kali (kemiripan semakin sama persis / identik)
    let lima = "5";
    let limo = 5;

    console.log(lima === limo);

